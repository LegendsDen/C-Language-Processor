integer main() 
begin
    integer a = 10;
    float b = 2.5e3;
    float c = -1.23E-2;
    char ch = 'A';
    char str[] = "Hello, World!";
    
    if (a > 5) a = a + 1;
    else   a = a - 1;

    for ( i = 0; i < 10; i++) 
    begin
        b = b / 2;
    end
    result=2<<1;
    integer z=++y;

    integer result = (x & y) | (x ^ y) + (x * y) / (x % y) -(--x);
    return 0;
end
